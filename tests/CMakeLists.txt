# Unit tests will be registered here as modules stabilize.
# Placeholder keeps the tests/ tree wired into CMake when BUILD_TESTING=ON.

message(STATUS "AiNpcSimulation: tests/ is reserved; no test targets yet.")
